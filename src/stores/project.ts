import { defineStore } from 'pinia'; let takeId=1, trackId=1; export const useProjectStore=defineStore('project',{ state:()=>({ bpm:120, audioCtx:new (window.AudioContext||(window as any).webkitAudioContext)(), tracks:[{id:'t'+trackId++, name:'Track 1', takes:[] as any[]}] }), actions:{ addTakeFromBlob(blob:Blob,name='Take'){ const url=URL.createObjectURL(blob); this.tracks[0].takes.push({id:'take'+(takeId++), url, name, ts:Date.now()}); } }});